# Hugging Face Space: Streamlit (full demo) + FastAPI (OpenEnv) behind nginx:7860
# No torch/trl (keep image smaller; use Colab for LLM)
streamlit>=1.28
matplotlib
pandas
fastapi>=0.100
uvicorn[standard]>=0.23
pydantic>=2
